spring:
  application:
    name: patrol-report-system

    mail:
      host: test.gmail.com
      port: 587
      username: test@email.com
      password: testpassword
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
server:
  address: 0.0.0.0
  port: 8080

